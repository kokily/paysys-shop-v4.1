import Layout from '../../components/closed/common/Layout';
import AddClosedContainer from '../../containers/closed/AddClosedContainer';

function AddClosedsPage() {
  return (
    <Layout>
      <AddClosedContainer />
    </Layout>
  );
}

export default AddClosedsPage;
