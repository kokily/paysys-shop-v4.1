import Layout from '../../components/closed/common/Layout';
import ReadClosedContainer from '../../containers/closed/ReadClosedContainer';

function ReadClosedsPage() {
  return (
    <Layout>
      <ReadClosedContainer />
    </Layout>
  );
}

export default ReadClosedsPage;
